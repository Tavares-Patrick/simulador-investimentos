// src/utils/investment.ts
export interface InvestmentResult {
  month: string;
  totalInvested: number;
  totalAccumulated: number;
}

// se tiver funções, exporte também:
export function calculateInvestment(...) {
  // lógica
}
